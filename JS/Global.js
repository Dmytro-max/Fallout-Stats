//let char
if(localStorage.getItem("storedChar")){
    console.log('restoration')
    // char = localStorage.getItem("storedChar")
    // SpecialBlockCreate(char),
    // SkillsBuild(char),
    // DerivedBuild(char),
    // TraitsBuild(char)
}